package com.match.calculator.repo;

import com.match.calculator.models.Cloth;
import org.springframework.data.repository.CrudRepository;

public interface ClothRepository extends CrudRepository<Cloth, Long> {
}
